import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: { flexDirection: "row", alignItems: "flex-end" },
  messageAvatarUrl: { width: 24, height: 24, marginRight: 8, borderRadius: 12, marginLeft: 32 },
  text: { fontSize: 10, lineHeight: 12, marginTop: 4 },
});
